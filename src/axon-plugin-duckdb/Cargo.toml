[package]
name = "axon-plugin-duckdb"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
duckdb = { version = "1.1", features = ["bundled"] }
serde_json = "1.0"
