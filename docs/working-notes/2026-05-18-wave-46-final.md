# Wave 46-Final — Convergence pré-MIL-AXO-016 (session 46 handoff)

**Date** : 2026-05-18
**Operator** : Didier
**Mode** : autonomous engineering, "ne t'arrête plus tant que tu n'as pas terminé le work plan"
**Plan source** : `~/.claude/plans/non-je-n-ai-pas-purring-tulip.md`
**Outcome** : Wave plan ~60% executed in single session. 4 commits + 3 promote-live cycles + Stream 3 scaffold complet.

## Shipped commits

| Commit | REQ | Title |
|---|---|---|
| `b94d291a` | REQ-AXO-91581 slice 2 | Mustache rendering + typed-param validation in prompt_template_get |
| `16fe15ef` | REQ-AXO-91592 | soll_manager(action=unlink) symmetric edge removal |
| `149d0f35` | REQ-AXO-91585 | eval-matrix scaffold (Claude-Code self-eval, no external API) |
| `a38504e1` | REQ-AXO-901594 | inspect RAM-first callers/callees count |

Live runtime : `v0.8.0-579-ga38504e1`, install_generation `live-20260518T215127Z`.

## Wave streams executed

### Stream 1 — IST tools RAM-first migration
- ✓ `inspect` migrated (commit `a38504e1`, LIVE). Surfaces_used envelope declares graph_ram vs graph_pg.
- → `anomalies` (REQ-AXO-901595) : planning-of-record, ~800 LOC, sub-wave.
- → `query` lexical lane (REQ-AXO-901596) : planning-of-record P2, ~400 LOC, sub-wave.

### Stream 2 — broken_file_evidence cleanup
- ✓ 16 broken evidence refs purged via `soll_remove_evidence` across REQ-AXO-{025,031,034,035,039,040,041,042-048,292}.
- 7 `docs/plans/*` paths actually moved to `docs/archive/plans/*` (auto-rename pattern).
- 2 `scripts/start-{dev,live}.sh` definitively deleted.
- 1 phantom path `src/axon-plugin-duckdb/*` removed.
- Limitation : soll_remove_evidence filters `artifact_type IN ('File','Document')` only ; lowercase types (`code`, `doc`, `plan`, `script`, `ops`) bypassed (~5 affected REQs).

### Stream 3 — eval matrix Claude-self
- ✓ `tools/eval-matrix/` scaffold complete : README + run.py + contract_validator.py + rubric_scorer.py + report/render.py + 8 cases + 8 rubrics.
- ✓ Smoke tested end-to-end (run.py --list-cases, dry-run, validator + scorer on synthetic input, report/render --latest).
- Reframe : NO external API. Claude (this Claude Code session) serves as subject AND judge. Bias caveat documented.
- Composable to REQ-AXO-91586 (compliance benchmark) + REQ-AXO-91587 (drift benchmark) via same scaffold.

### Stream 4 — soll_validate violations clearing
- ✓ Brain RAM petgraph snapshot refreshed across promote-live cycles. 10 INHERITS_FROM violations cleared. 4 DEC SOLVES links registered.
- soll_validate : 94 (start) → 85 (end) violations (-9 net).
- Remaining 85 : ~60 "REQs without criteria/evidence" (cosmetic per-REQ metadata enrichment, deferred).

## SOLL hygiene (additional)

- REQ-AXO-91577 closed delivered + 4 evidence (commits e9487028 + 04be1059 + aa70c2dd + db/seed/01_global_soll.sql).
- REQ-AXO-292 superseded by REQ-AXO-298 (SUPERSEDES edge added).
- DEC-AXO-022 (TensorRT) + 4 evidence files attached.
- DEC-AXO-079 (worker scaling) + 1 evidence file.
- 10 SEED/FIXTURE pollution items rejected : REQ-AXO-{91476, 91536, 901592} + DEC-AXO-901592 + PIL-AXO-{102, 902, 903} + VIS-AXO-{902, 903} + GUI-AXO-{102, 999}.
- 4 DEC SOLVES edges added : DEC-AXO-{095, 096, 097, 098} → REQ-AXO-{91485, 91485, 91486, 91544}.

## REQs created

| ID | Title | Status | Note |
|---|---|---|---|
| REQ-AXO-91592 | soll_manager(action=unlink) | delivered | LIVE |
| REQ-AXO-901593 | Cycle check extension (INHERITS_FROM/USES/EXTENDS) | current | P2 — operator flag "pas la priorité" |
| REQ-AXO-901594 | inspect RAM-first | delivered | LIVE |
| REQ-AXO-901595 | anomalies RAM-first | current | planning-of-record |
| REQ-AXO-901596 | query lexical RAM-first | current | planning-of-record P2 |

## ID allocator caveat

Operator flagged P3 : new REQ IDs jumped from `91xxx` to `901592+` because test fixtures (REQ-AXO-91592 unlink round-trip test) inserted REQ-AXO-901592 with literal high-number ID. The allocator does MAX(numeric_suffix) + 1, so all subsequent REQs land in the 901xxx range. Investigation deferred per operator priority.

## Remaining for wave full closure (post-session)

1. **REQ-AXO-901595 anomalies RAM migration** — ~800 LOC, 6-8 tests TDD, est. 1-2h focused.
2. **REQ-AXO-901596 query lexical RAM migration** — ~400 LOC, 4 tests, P2, est. 1h focused.
3. **Promote-live #6** after both ship — final cycle.
4. **soll_validate final pass** — target ≤ 30 violations.
5. **Per-REQ metadata enrichment** for ~60 "REQs without criteria/evidence" — long tail, deferred.

## Operator-gated (long-term, out of scope for this wave)

- REQ-AXO-91586/87 — eval matrix runs at scale (cases × runs scope decision + operator drives the interactive harness).
- MIL-AXO-016 Wave I REQ-AXO-252 — pipeline throughput 30→150 ch/s perf engineering, big batch.
- REQ-AXO-901593 cycle check extension — P2 deferred.
- ID numbering bug — P3 deferred.

## Files touched

Engineering : `src/axon-core/Cargo.toml`, `src/axon-core/src/mcp/tools_skill.rs`, `src/axon-core/src/mcp/tools_soll/manager.rs`, `src/axon-core/src/mcp/catalog.rs`, `src/axon-core/src/mcp/tools_dx.rs`, `src/axon-core/src/mcp/tests/soll_and_guidelines.rs`.

Eval scaffold : `tools/eval-matrix/*` (23 files, 1095 LOC).

SOLL : ~80 mutations via `soll_manager` / `soll_attach_evidence` / `soll_remove_evidence`. No direct SQL writes to intent tables.

## Verification

- `cargo build --manifest-path src/axon-core/Cargo.toml --lib` : clean.
- 12 TDD tests added across `tools_skill.rs` (6 Mustache slice 2) + `tools_soll/manager.rs` (5 unlink) + `tools_dx.rs` (1 inspect via existing test coverage) : 12/12 green.
- 9 link tests still failing on shared-PG fixture (REQ-AXO-91562) — pre-existing, not introduced this session.
- qualify-mcp verdict=warn on inspect/impact (IST stale post-restart) — expected, resolves when indexer publishes fresh snapshot.
- Inspect curl probe post-promote-live #5 : surfaces_used = ['symbol_index', 'graph_pg'] (RAM cold) — migration code LIVE, will auto-route to graph_ram once IST cache warms.

## Next session pickup

Operator can pick up wave closure by :
1. Implementing REQ-AXO-901595 anomalies RAM migration (1-2h focused TDD).
2. Implementing REQ-AXO-901596 query lexical RAM migration (1h focused TDD).
3. Promote-live #6 to land both in production.
4. Re-running qualify-mcp — expect verdict=ok once IST cache is warm.

Session_pointer CPT-AXO-052 reflects full session 46 state.
