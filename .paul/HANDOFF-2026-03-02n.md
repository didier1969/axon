# PAUL Handoff

**Date:** 2026-03-02
**Status:** paused — Plan 03-01 APPLY complete, UNIFY not yet run

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** axon — graph-powered code intelligence engine. Indexes any codebase into KuzuDB; AI agents query via MCP instead of reading raw files.
**Core value:** Developers and AI agents instantly understand any codebase — files auto-indexed, agents query the DB to reduce token usage.

---

## Current State

**Version:** 0.6.0 (in progress)
**Milestone:** v0.6 Daemon & Centralisation
**Phase:** 3 of 3 — Watch & filtrage (in progress)
**Plan:** 03-01 APPLY complete — UNIFY pending

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     [03-01 executed, needs UNIFY]
```

---

## What Was Done (this session)

- **PLAN 03-01** created: `.paul/phases/03-watch-filtrage/03-01-PLAN.md`
  - Phase 3 scoped into 3 plans: 03-01 (filters+debounce), 03-02 (queue), 03-03 (byte-offset)
- **APPLY 03-01** complete:
  - Added `.paul/` to `DEFAULT_IGNORE_PATTERNS` in `src/axon/config/ignore.py`
  - Added `_make_watch_filter()` helper in `src/axon/core/ingestion/watcher.py`
    - watchfiles `watch_filter` param pre-filters ignored paths before they enter the loop
  - Added `debounce_ms: int = 500` param to `watch_repo()`
  - Added `--debounce INTEGER` CLI option to `axon watch` and `axon serve --watch`
  - Fixed all pre-existing ruff lint errors in modified files (E501, E402, I001)
  - 821 tests, 0 failures (+3 new tests); 0 lint errors

---

## What's In Progress

UNIFY 03-01 not yet run — need to create SUMMARY and close the loop.

---

## What's Next

**Immediate:** `/paul:unify .paul/phases/03-watch-filtrage/03-01-PLAN.md`

**After that:**
- Plan + Apply + Unify 03-02 (sequential watcher queue with asyncio.Queue)
- Plan + Apply + Unify 03-03 (byte-offset caching: start_byte/end_byte in schema)
- `/paul:complete-milestone` → v0.7

---

## Architecture Context

```
Phase 3 plans:
  03-01 (done): filters + debounce
    → .paul/ now ignored (alongside .git, .axon)
    → watch_filter pre-filters at watchfiles level
    → debounce_ms param + --debounce CLI flag

  03-02 (next): sequential queue
    → asyncio.Queue in watch_repo()
    → awatch loop puts batches, consumer drains sequentially
    → prevents concurrent KuzuDB writes under rapid changes

  03-03 (planned): byte-offset caching
    → add start_byte/end_byte to SymbolInfo + GraphNode + KuzuDB schema
    → all parsers already have node.start_byte/node.end_byte from tree-sitter
    → enables O(1) symbol content read: file[start_byte:end_byte]
```

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/phases/03-watch-filtrage/03-01-PLAN.md` | Current plan (APPLY complete) |
| `src/axon/core/ingestion/watcher.py` | Watcher — updated with filter + debounce |
| `src/axon/config/ignore.py` | Ignore patterns — .paul/ added |
| `src/axon/cli/main.py` | CLI — --debounce flag added |

## Git State

Branch: main
Uncommitted: Plan 03-01 changes (5 source files + 2 test files + PAUL files)

---

## Resume Instructions

1. Run `/paul:resume` — it will find this handoff
2. Loop is at PLAN ✓ APPLY ✓ UNIFY ○ for plan 03-01
3. Run `/paul:unify .paul/phases/03-watch-filtrage/03-01-PLAN.md`

---

*Handoff créé: 2026-03-02*
