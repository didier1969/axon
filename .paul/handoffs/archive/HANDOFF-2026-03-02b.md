# PAUL Handoff

**Date:** 2026-03-02
**Status:** paused — Plan 02-01 APPLY complete, UNIFY not yet run

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** axon — graph-powered code intelligence engine. Indexes any codebase into KuzuDB; AI agents query via MCP instead of reading raw files.
**Core value:** Developers and AI agents instantly understand any codebase — files auto-indexed, agents query the DB to reduce token usage.

---

## Current State

**Version:** 0.7.0 (in progress)
**Milestone:** v0.7 Quality & Security
**Phase:** 2 of 2 — Qualité, Parsers & Features (in progress)
**Plan:** 02-01 APPLY complete — UNIFY pending

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     [02-01 executed, needs UNIFY]
```

---

## What Was Done (this session)

- **Stale handoff archived** (HANDOFF-2026-03-02f.md → .paul/handoffs/archive/)
- **Phase 2 plan created:** `.paul/phases/02-qualite-parsers-features/02-01-PLAN.md`
  - 3 tasks: sql byte offsets, yaml byte offsets, axon_read_symbol MCP tool
- **APPLY 02-01 complete** — commit `31bd23e`:
  - `sql_lang.py`: `start_byte = m.start()`, `end_byte = content.find(';', start_byte) + 1` for all CREATE symbols
  - `yaml_lang.py`: pre-computed `line_start_bytes[]` via `splitlines(keepends=True)` passed to both `_parse_yaml()` and `_parse_toml()`
  - `mcp/tools.py`: `handle_read_symbol()` + `_get_repo_root_from_storage()` helper
  - `mcp/server.py`: `axon_read_symbol` in TOOLS list + `_dispatch_tool()` dispatch
- **852 tests pass**, 0 new ruff errors

---

## What's In Progress

UNIFY 02-01 not yet run — need to create SUMMARY and close the loop.

---

## What's Next

**Immediate:** `/paul:unify .paul/phases/02-qualite-parsers-features/02-01-PLAN.md`

**After that (plans 02-02 through 02-04):**

| Plan | Scope | Key files |
|------|-------|-----------|
| 02-02 | Parser quality: dead code test patterns + TS generics + Python wildcard imports | dead_code.py, typescript.py, python_lang.py |
| 02-03 | Architecture: file size limit (walker.py) + compute_repo_slug() helper + AXON_LRU_SIZE env var | walker.py, cli/main.py, core/paths.py, daemon/server.py |
| 02-04 | MCP polish: socket buffer makefile/readline + axon_batch partial failure + tool descriptions | mcp/server.py |

---

## Architecture Context

```
axon_read_symbol (new tool):
  - Query: MATCH (n) WHERE n.name = $name [AND n.file_path CONTAINS $file]
  - If start_byte > 0: read repo_root/file_path bytes[start:end]
  - repo_root via: storage.db_path.parent/meta.json → data["path"]
  - Fallback: stored content field in graph node

sql/yaml byte offsets:
  - sql_lang.py: regex m.start() → start_byte; scan for ';' → end_byte
  - yaml_lang.py: line_start_bytes[] = cumulative UTF-8 encoded line lengths
```

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/phases/02-qualite-parsers-features/02-01-PLAN.md` | Completed plan (APPLY done) |
| `src/axon/core/parsers/sql_lang.py` | SQL parser — now has byte offsets |
| `src/axon/core/parsers/yaml_lang.py` | YAML/TOML parser — now has byte offsets |
| `src/axon/mcp/tools.py` | MCP handlers — handle_read_symbol added |
| `src/axon/mcp/server.py` | MCP server — axon_read_symbol registered |

## Git State

Branch: main
Last commit: `31bd23e` (feat(02-qualite-parsers-features): 02-01 — sql/yaml byte offsets + axon_read_symbol)
Uncommitted: PAUL files only (STATE.md, this handoff)

---

## Resume Instructions

1. Run `/paul:resume` — it will find this handoff
2. Loop is at PLAN ✓ APPLY ✓ UNIFY ○ for plan 02-01
3. Run `/paul:unify .paul/phases/02-qualite-parsers-features/02-01-PLAN.md`

---

*Handoff créé: 2026-03-02*
