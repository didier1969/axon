# PAUL Handoff

**Date:** 2026-03-02
**Status:** paused — Phase 2 complete, Phase 3 not yet started

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** axon — graph-powered code intelligence engine. Indexes any codebase into KuzuDB; AI agents query via MCP instead of reading raw files.
**Core value:** Developers and AI agents instantly understand any codebase — files auto-indexed, agents query the DB to reduce token usage.

---

## Current State

**Version:** 0.6.0 (in progress)
**Milestone:** v0.6 Daemon & Centralisation
**Phase:** 3 of 3 — Watch & filtrage (not started)
**Plan:** Not started

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Phase 2 complete — ready for Phase 3 PLAN]
```

---

## What Was Done (this session)

- **APPLY 02-03** — `axon_batch` MCP tool implemented:
  - `_batch_daemon_call()`: 1 socket, N sequential send/recv, returns formatted block or None
  - `call_tool()` special-cases `axon_batch` (daemon-first + direct fallback + per-sub-result max_tokens)
  - `axon_batch` added to `TOOLS` with correct schema (calls required, max_tokens optional)
  - 6 new tests in `TestBatchTool`, 818 total (was 812), 0 failures
  - All pre-existing ruff E501s fixed — 0 lint errors now
- **UNIFY 02-03** — SUMMARY created, STATE/PROJECT/ROADMAP updated
- **Phase 2 transition** — Phase 2 marked complete, transitioned to Phase 3
  - 5 stale handoffs archived to `.paul/handoffs/archive/`
  - Committed: `d5d278d` — `feat(02-daemon-central): complete Phase 2`

---

## What's In Progress

Nothing — Phase 2 fully complete. Phase 3 not started.

---

## What's Next

**Immediate:** `/paul:plan` for Phase 3 (Watch & filtrage)

**Phase 3 scope (from ROADMAP):** Sequential watcher queue with priority, configurable debounce, built-in filters (`.paul/`, `_build/`, `target/` by default). Byte-offset caching for O(1) symbol recovery.

**After Phase 3:** v0.6 milestone complete → `/paul:complete-milestone` → v0.7 (BM25 hybrid search: FTS5 + vector + graph distance)

---

## Architecture Context

```
v0.6 Phase 2 — What's now in place:
  axon daemon start/stop/status
    → Unix socket ~/.axon/daemon.sock
    → LRU cache (max 5 KuzuDB connections)
    → Popen(start_new_session=True) for orphan process

  MCP server (N proxy processes, ~10MB each)
    → call_tool(): daemon-first (5s timeout), direct fallback
    → axon_batch: 1 socket, N sequential send/recv

  Central storage: ~/.axon/repos/{slug}/kuzu
```

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview — Phase 3 is next |
| `.paul/phases/02-daemon-central/02-03-SUMMARY.md` | What was built in Phase 2 Plan 3 |
| `src/axon/mcp/server.py` | MCP server with axon_batch |
| `src/axon/daemon/server.py` | Daemon server (complete, no changes needed) |

## Git State

Last commit: `d5d278d` (feat(02-daemon-central): complete Phase 2)
Branch: main
Uncommitted: uv.lock (minor, ignore)

---

## Resume Instructions

1. Run `/paul:resume` — it will find this handoff
2. Loop is at PLAN ✓ APPLY ✓ UNIFY ✓ (Phase 2 done)
3. Run `/paul:plan` for Phase 3 (Watch & filtrage)

---

*Handoff créé: 2026-03-02*
